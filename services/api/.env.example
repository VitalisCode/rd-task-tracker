APP_ENV=production
SECRET_KEY=changeme         # replace with the real secret value in production
DB_PASSWORD=changeme        # replace with the real DB password in production
JWT_SECRET=changeme         # replace with the real JWT secret in production
LOG_LEVEL=INFO

# In production, inject these same env vars into the pod from AWS Secrets Manager or your secret injection mechanism.